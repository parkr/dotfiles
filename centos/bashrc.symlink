# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi

# User specific aliases and functions

export LD_LIBRARY_PATH="/usr/local/rvm/usr/lib"
if [ -f /usr/local/rvm/scripts/rvm ]; then
	. /usr/local/rvm/scripts/rvm
fi
